name: Build

on:
  push:
  pull_request:

jobs:
  macos:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v7
      - name: Build
        run: make
      - name: Test
        run: make test
      - name: Verify CLI
        run: ./build/horndis --version
